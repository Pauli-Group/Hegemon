[package]
name = "cashvm-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
consensus-light-client = { path = "../../consensus-light-client" }
codec = { workspace = true }
protocol-kernel = { path = "../../protocol/kernel" }
sha2 = "0.10"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
