[package]
name = "protocol-versioning"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }
sha2 = { version = "0.10", default-features = false }

[features]
default = ["std"]
std = ["serde/std", "sha2/std"]
