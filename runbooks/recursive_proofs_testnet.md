# Recursive Epoch Proofs (Deprecated)

Recursive epoch proofs were removed during the Plonky3 migration. This runbook is archived and should not be used on current builds.

If recursive epoch proofs are reintroduced with a Plonky3 recursion path, this runbook will be rewritten to reflect the new protocol and CLI.
