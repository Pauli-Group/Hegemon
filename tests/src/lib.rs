//! Workspace-level security orchestration crate for integration tests.

pub fn init() {
    // Intentionally empty placeholder so the crate builds even though
    // all of the behavior lives in the integration tests declared in
    // Cargo.toml.
}
